# Backend API URL — used to construct API request base and resolve /uploads image paths
# For local development with Vite proxy, leave empty or omit this file
# For production, set to your deployed backend URL:
VITE_API_URL=https://your-backend.onrender.com/api
